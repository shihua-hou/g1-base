# g1_teach_v2 目录与代码结构说�?
本文档旨在详细介�?`g1_teach_v2` 项目的目录结构和各个核心代码文件的作用，帮助开发者快速理解项目的内部运作机制�?
## 📁 顶层目录结构

项目根目�?`g1_teach_v2/` 包含以下几类重要的子目录和文件：

### 数据与配置存�?
这三个目录构成了示教工具的数据流基础�?
*   **`movement/motions/`**：存放录制好的连续动作轨迹文件（`.jsonl` 格式）。文件包含带时间戳的关节位置数据流�?*   **`movement/snapshots/`**：存放捕获的关键姿态文件（`.json` 格式）。文件保存机器人特定状态下的单帧关节角度�?*   **`movement/scripts/`**：存放编排好的动作流程脚本文件（`.json` 格式）。脚本用于定义基于时间序列的复合动作流（包含 motion、snapshot、hold、hand 等步骤）�?
## 📄 核心源码结构

### 1. 入口与命令行解析
*   **`__init__.py`**：将当前目录标识�?Python 包�?*   **`__main__.py`**：允许项目以 `python -m g1_teach_v2` 的形式直接运行，入口重定向到 `cli.py`�?*   **`cli.py`**：整个项目的命令行接口（Command Line Interface）。它使用 `argparse` 定义了所有可用的子命令（�?`record-motion`, `capture-snapshot`, `play-motion`, `run-script` 等）及其参数，并将调用路由到具体的业务逻辑函数�?
### 2. 核心业务逻辑
*   **`motion_io.py`**：处理连续动作轨迹的录制、保存、加载与回放逻辑。它负责解析 `.jsonl` 文件并将其转化为发送给机器人的控制指令流�?*   **`snapshot_io.py`**：处理关键帧姿态的接管、捕获与保存，以及负责把机器人从当前姿态平滑插值过渡（goto）到目标 snapshot 姿态的逻辑�?*   **`script_runner.py`**：动作流脚本执行引擎。负责解�?`scripts/` 中的复杂编排文件，依序循环执行各�?step（如调用 motion 回放、调�?snapshot 移动、停顿等�?`hold`，或发送灵巧手指令 `hand` 等）�?*   **`record_modes.py`**：实现了多种不同的动作录制策略（例如 `teach_upper` 双臂跟随示教，`right_hold_left` 保持左臂只拖动右臂，`raw` 纯数据状态采样，以及限制手腕转动等特定约束）�?
### 3. 底层与控制抽�?*   **`robot_io.py`**：封装与机器人底�?SDK 的通信。负责建立网卡连接、数据状态的收发（获�?state、发�?command）�?*   **`joints.py`**：维护了机器人关节的名称、ID 映射关系以及分组规则（例如明确定义了左臂、右臂、腰部等关节组的具体组成），方便上层功能针对特定部位操作�?*   **`profiles.py`**：定义并存放控制相关的增益配置（Profile），例如在不同的回放或关节保持状态下所使用的位置和速度 PD（比�?微分）参数值�?*   **`takeover_debug.py`**：包含了控制权接管时的诊断日志逻辑，以及前馈扭矩（`tau_ff`）的平滑插值渐退策略。这是为了减少切换到 `arm_sdk` 接管瞬间手臂的“下坠感”或震荡�?
### 4. 外部设备扩展
*   **`hand_adapters.py`**：灵巧手硬件适配器。目前用于通过外部 DDS 桥接通信，控制因时（Inspire）FTP 灵巧右手，执行编排脚本中的特定手势指令（�?`count_1`）或设定的指尖开合目标值�?
## 📖 文档�?*   **`README.md`**：英文用户手册�?*   **`README.zh.md`**：中文用户手册，详细介绍了完整的命令行操作工作流、运行参数以及脚本（Script）的高级编写语法�?
