# 默认路线：出厂状态下必须是空的。
#
# 这里原本放着一个演示点（9.862, -5.135 @ 51.815，动作 31，"企业数据治理体系"），
# 它会被 start_g1_base.sh 的 seed_data 播种到数据卷，于是现场每次打开
# 「多点巡航」都凭空多出一个谁也没加过的巡航点，还指向别的场馆的坐标——
# 误点「开始巡航」机器人就会往一个不存在的地方走。
#
# 演示数据仍保留在 waypoint_1/2/3.yaml 和 recorded_waypoints.yaml 里，
# 需要的话在界面上「切换路线」即可，不会丢。
route_name: "default"
waypoints: []
